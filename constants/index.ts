import { Fugaz_One, Open_Sans } from "next/font/google";

export const OPENSANS_FONT = Open_Sans({ subsets: ["latin"] });
export const FUGAZ_FONT = Fugaz_One({ subsets: ["latin"], weight: ["400"] });
